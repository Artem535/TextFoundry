= Update Blocks To Latest Prompt Template

Для этой capability отдельный prompt-template не нужен.

Причина:

* функция deterministic;
* не использует LLM provider;
* не требует prompt assembly.

Страница сохранена для единообразия function pipeline.
