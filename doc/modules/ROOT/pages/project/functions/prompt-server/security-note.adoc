= Prompt Server Security Note

== 1. Security Scope

Server exposes published prompt assets to clients, so the primary risks are
access control, data boundary violations and unsafe parameter handling.

== 2. Key Requirements

Ключевые требования:

* только published data
* no draft mutation
* parameter input validation
* authn/authz policy для external clients
